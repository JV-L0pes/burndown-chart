{
  "name": "burndown-chart",
  "version": "1.0.0",
  "description": "Dashboard de Burndown Chart integrado com Trello",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 3000",
    "build": "echo 'Build completed'",
    "deploy": "gh-pages -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chart.js": "^4.4.1",
    "dotenv": "^16.3.1",
    "lucide": "^0.294.0",
    "aos": "^2.3.4"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1"
  }
}
